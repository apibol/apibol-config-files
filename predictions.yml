---
info:
  component: Prediction MicroService

server:
  port: 0

eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8761/eureka/

data:
  mongodb:
    host: localhost
    port: 27017
    database: prediction
    username: mongo
    password: admin

services:
  user:
    info: http://USERS/user/
  event:
    info: http://EVENTS/event/
    game:
      info: http://EVENTS/event/{0}/game/{1}
  predictor:
      info: http://PREDICTORS/predictor/
      participant-info: http://PREDICTORS/predictor/{0}/participant/{1}

rabbit:
  queue:
    userpoints: userpoints
    results: results
  exchange:
    userpoints: userpoints
    event: event
